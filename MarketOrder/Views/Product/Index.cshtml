﻿@using MarketOrder.Models
@model IEnumerable<ProductView>

@{
    ViewBag.Title = "Index";
}

@section Scripts {
    <script src="~/Scripts/js/home.js"></script>
}

<div id="divMsg"></div>
<br />
<button type="button" class="btn btn-primary btn-md" onclick="ClickMeButton()">
    Show Message
</button>

<div id="divMessages" class="messages">
</div>
<div class="input-zone">
    <label id="lblMessage" for="tbMessage">Message:</label>
    <input id="tbMessage" class="input-zone-input" type="text" />
    <button id="btnSend">Send</button>
</div>

<h1>Making an order</h1>

@Html.DropDownListFor(m => m, Model.UserGroups.Select(x => new SelectListItem() { Text = x.Name, Value = x.Id }), new { @class = "pad5" })
